# Workspace Docs

이 디렉토리는 `rescue-robot-workspace`의 보조 문서와 정리된 산출물을 보관하는 용도입니다.

## 보관 원칙

- 루트 `README.md`에는 워크스페이스 개요와 실행 흐름만 유지합니다.
- 패키지별 실행 계약이나 세부 설계 메모는 각 패키지의 `docs/` 아래에 둡니다.
- 발표 자료, 코드 리뷰 산출물, 실험 노트는 필요 시 `docs/archive/`로 분리합니다.
- 모델 가중치, 데이터베이스, 오디오 파일 같은 런타임 자산은 문서 디렉토리에 두지 않습니다.
